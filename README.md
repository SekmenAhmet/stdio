# stdio (x86-64 asm)

Projet perso pour reimplementer une mini partie de `stdio.h` en assembleur x86-64 (Linux),
pour apprendre les bases du code bas niveau.

## Structure

- `print/` : code asm + exemple C, et instructions de build dans `run.md`.

## Prerequis

- `nasm`
- `ld`
- `gcc`

